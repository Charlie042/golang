services:
  db:
    image: postgres:16
    container_name: todo_api_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Ginger200598
      POSTGRES_DB: todo_api_g
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
